/** @odoo-module **/

/**
 * PedidosYa POS Frontend - punto de entrada.
 * La lógica del botón está en pedidosya_floor_patch.js (HTML/JS puro).
 * Este archivo existe para que el manifest lo cargue en el bundle del POS.
 */

import "./pedidosya_floor_patch";
